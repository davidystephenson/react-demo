import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// This is the only important line
// It means "the 'root' element into a React app"
ReactDOM.render(<App />, document.getElementById('root'));